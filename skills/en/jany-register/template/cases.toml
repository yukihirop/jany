# jany cases: __NAME__. Run by `jany --test __NAME__`.
# Expectations: argv / preview / risk / pipe / error / confidence / tokens / state / not_asked / setup.dirs / no_jev
# [case.jev] is a Mock of jev's answers. Answering a key that was not asked fails (see what is asked with --explain).

[[case]]
words = ["-v"]
argv  = [__ARGV0__, "-v"]
no_jev = true

# [[case]]
# words = ["src", "big"]
# argv  = [__ARGV0__, "src", "--big"]
# [case.jev]
# "role.1" = { choice = "option_word", confidence = 0.9 }
